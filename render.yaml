services:
  - type: web
    name: news-recommender
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn app:app"
    envVars:
      - key: MONGO_URI
        sync: false
      - key: NEWS_API_KEY
        sync: false
